"""Parts of the autodev orchestrator that stand on their own.

`scripts/autodev.py` stays the entry point and re-exports everything here, so the modules are an
organising device rather than a public interface. Stdlib only, Python 3.9+.
"""
